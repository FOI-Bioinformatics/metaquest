[flake8]
max-line-length = 120
max-complexity = 15
# Test fakes are dispatch tables by design; the CI lints metaquest only.
per-file-ignores =
    tests/*:C901
extend-ignore = E203, W503
exclude = build,dist,.eggs,.git,__pycache__,.tox,.venv,venv